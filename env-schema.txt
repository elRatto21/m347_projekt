JDBC_DATABASE_URL=jdbc:postgresql:/[db-host]:[port]/[db-name]
JDBC_DATABASE_USERNAME=[db-username]
JDBC_DATABASE_PASSWORD=[db-password]
JWT_ISSUER_URI=http://[keycloak-host]:[port]/realms/[realm-name]
POSTGRES_USER=[db-username]
POSTGRES_PASSWORD=[db-password]
POSTGRES_DB=[db-name]
KEYCLOAK_USER=[keycloak-username]
KEYCLOAK_PASSWORD=[keycloak-password]
PGADMIN_DEFAULT_EMAIL=[pgadmin-email]
PGADMIN_DEFAULT_PASSWORD=[pgadmin-password]